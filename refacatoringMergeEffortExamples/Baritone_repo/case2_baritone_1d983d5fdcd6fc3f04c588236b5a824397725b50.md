# Case 2 — Project: baritone — Merge commit SHA1: 1d983d5fdcd6fc3f04c588236b5a824397725b50

---

## Modified file(s):
- `src/api/java/baritone/api/schematic/mask/Mask.java`
- `src/api/java/baritone/api/schematic/mask/StaticMask.java`
- `src/api/java/baritone/api/schematic/mask/operator/BinaryOperatorMask.java`
- `src/api/java/baritone/api/schematic/mask/operator/NotMask.java`
- `src/api/java/baritone/api/schematic/mask/shape/CylinderMask.java`

---

## Class(es) modified in the merge:
`Mask` (interface), `StaticMask` (interface), `BinaryOperatorMask`, `NotMask`, `CylinderMask`

---

## Merge effort lines in the combined diff

In `Mask.java`:
```diff
-- boolean partOfMask(int x, int y, int z, IBlockState currentState);
++ boolean partOfMask(int x, int y, int z, BlockState currentState);
```

In `StaticMask.java`:
```diff
-- default boolean partOfMask(int x, int y, int z, IBlockState currentState) {
++ default boolean partOfMask(int x, int y, int z, BlockState currentState) {
```

In `BinaryOperatorMask.java`:
```diff
-- public boolean partOfMask(int x, int y, int z, IBlockState currentState) {
++ public boolean partOfMask(int x, int y, int z, BlockState currentState) {

-- private static boolean partOfMask(Mask mask, int x, int y, int z, IBlockState currentState) {
++ private static boolean partOfMask(Mask mask, int x, int y, int z, BlockState currentState) {
```

In `NotMask.java`:
```diff
-- public boolean partOfMask(int x, int y, int z, IBlockState currentState) {
++ public boolean partOfMask(int x, int y, int z, BlockState currentState) {
```

In `CylinderMask.java` — field and constructor parameter:
```diff
-- private final EnumFacing.Axis alignment;
++ private final Direction.Axis alignment;

-- public CylinderMask(int widthX, int heightY, int lengthZ, boolean filled, EnumFacing.Axis alignment) {
++ public CylinderMask(int widthX, int heightY, int lengthZ, boolean filled, Direction.Axis alignment) {

-- private static int getA(int x, int y, EnumFacing.Axis alignment) {
--     return alignment == EnumFacing.Axis.X ? y : x;
++ private static int getA(int x, int y, Direction.Axis alignment) {
++     return alignment == Direction.Axis.X ? y : x;

-- private static int getB(int y, int z, EnumFacing.Axis alignment) {
--     return alignment == EnumFacing.Axis.Z ? y : z;
++ private static int getB(int y, int z, Direction.Axis alignment) {
++     return alignment == Direction.Axis.Z ? y : z;
```

---

## Relevant final code in the merge

```java
// Mask.java
boolean partOfMask(int x, int y, int z, BlockState currentState);

// StaticMask.java
@Override
default boolean partOfMask(int x, int y, int z, BlockState currentState) {
    return this.partOfMask(x, y, z);
}

// BinaryOperatorMask.java
@Override
public boolean partOfMask(int x, int y, int z, BlockState currentState) { ... }

private static boolean partOfMask(Mask mask, int x, int y, int z, BlockState currentState) { ... }

// NotMask.java
@Override
public boolean partOfMask(int x, int y, int z, BlockState currentState) { ... }

// CylinderMask.java
private final Direction.Axis alignment;
public CylinderMask(int widthX, int heightY, int lengthZ, boolean filled, Direction.Axis alignment) { ... }
private static int getA(int x, int y, Direction.Axis alignment) { ... }
private static int getB(int y, int z, Direction.Axis alignment) { ... }
```

---

## Number of merge-effort lines (`++` and `--`) associated with the refactoring types under analysis:
**18 lines**

---

## What each side had

**Parent 1** used the old Minecraft Forge API types: `IBlockState` as the parameter type in `partOfMask` across the entire `Mask` hierarchy, and `EnumFacing.Axis` in `CylinderMask`. These are 1.12.x Minecraft/Forge API types.

**Parent 2** had migrated to the modern Minecraft API: `BlockState` (replacing `IBlockState`) and `Direction.Axis` (replacing `EnumFacing.Axis`). This migration was applied consistently across the entire `Mask` interface hierarchy and `CylinderMask`.

---

## Interpretation

P2 performed a **Change_Parameter_Type** refactoring across the entire `Mask` interface hierarchy and `CylinderMask`, migrating from legacy Forge API types to modern Minecraft API types:

1. **`IBlockState` → `BlockState`** in `partOfMask` across `Mask`, `StaticMask`, `BinaryOperatorMask`, and `NotMask` — four method signatures and one private static helper changed consistently.

2. **`EnumFacing.Axis` → `Direction.Axis`** in `CylinderMask` — field declaration, constructor parameter, and two private static helpers all changed type simultaneously.

The merge had to reconcile P1's legacy types with P2's modern types, producing `--`/`++` lines at every affected declaration site. This is a well-defensible **Change_Parameter_Type** applied systematically across an interface hierarchy and its concrete implementations — a cross-cutting type migration that required coordinated `++`/`--` merge effort at 9 distinct locations across 5 files.

Additionally, the `Rename_Attribute` on `CylinderMask.alignment` from `EnumFacing.Axis` to `Direction.Axis` type represents a **Change_Parameter_Type** at the field level as well.
