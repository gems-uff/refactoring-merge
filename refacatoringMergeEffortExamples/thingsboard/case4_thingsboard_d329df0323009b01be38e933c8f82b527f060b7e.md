# Case 2 — Project: thingsboard — Merge commit SHA1: d329df0323009b01be38e933c8f82b527f060b7e

---

## Modified file(s):
`application/src/main/java/org/thingsboard/server/service/edge/EdgeEventSourcingListener.java`

---

## Class(es) modified in the merge:
`EdgeEventSourcingListener`

---

## Merge effort lines in the combined diff

```diff
-- private boolean isValidEdgeEventEntity(SaveEntityEvent<?> event) {
++ private boolean isValidSaveEntityEventForEdgeProcessing(SaveEntityEvent<?> event) {
++     Object entity = event.getEntity();
++     Object oldEntity = event.getOldEntity();
++         if (entity instanceof RuleChain) {
++             RuleChain ruleChain = (RuleChain) entity;
++             return RuleChainType.EDGE.equals(ruleChain.getType());
++         }
++         break;
++         if (entity instanceof User) {
++             User user = (User) entity;
++             if (Authority.SYS_ADMIN.equals(user.getAuthority())) {
++                 return false;
++             }
++             if (oldEntity != null) {
++                 User oldUser = (User) oldEntity;
++                 cleanUpUserAdditionalInfo(oldUser);
++                 cleanUpUserAdditionalInfo(user);
++                 return !user.equals(oldUser);
++             }
++         }
++         break;
++         if (entity instanceof AlarmApiCallResult || entity instanceof Alarm) {
++             return false;
++         }
++         break;
```

Also at call sites:
```diff
-- if (isValidEdgeEventEntity(event)) {
++ if (isValidSaveEntityEventForEdgeProcessing(event)) {
```

And at the sendNotificationMsgToEdge call sites:
```diff
-- EdgeUtils.getEdgeEventActionTypeByActionType(event.getActionType()));
++ EdgeUtils.getEdgeEventActionTypeByActionType(event.getActionType()),
++ edgeSynchronizationManager.getEdgeId().get());

-- JacksonUtil.toString(relation), EdgeEventType.RELATION, EdgeUtils.getEdgeEventActionTypeByActionType(event.getActionType()));
++ JacksonUtil.toString(relation), EdgeEventType.RELATION, EdgeUtils.getEdgeEventActionTypeByActionType(event.getActionType()),
++ edgeSynchronizationManager.getEdgeId().get());
```

---

## Relevant final code in the merge

```java
private boolean isValidSaveEntityEventForEdgeProcessing(SaveEntityEvent<?> event) {
    Object entity = event.getEntity();
    Object oldEntity = event.getOldEntity();
    switch (event.getEntityId().getEntityType()) {
        case RULE_CHAIN:
            if (entity instanceof RuleChain) {
                RuleChain ruleChain = (RuleChain) entity;
                return RuleChainType.EDGE.equals(ruleChain.getType());
            }
            break;
        case USER:
            if (entity instanceof User) {
                User user = (User) entity;
                if (Authority.SYS_ADMIN.equals(user.getAuthority())) {
                    return false;
                }
                if (oldEntity != null) {
                    User oldUser = (User) oldEntity;
                    cleanUpUserAdditionalInfo(oldUser);
                    cleanUpUserAdditionalInfo(user);
                    return !user.equals(oldUser);
                }
            }
            break;
        case ALARM:
            if (entity instanceof AlarmApiCallResult || entity instanceof Alarm) {
                return false;
            }
            break;
        // ...
    }
}
```

---

## Number of merge-effort lines (`++` and `--`) associated with the refactoring types under analysis:
**22 lines**

---

## What each side had

**Parent 1** had the method named `isValidEdgeEventEntity(SaveEntityEvent<?> event)` with a simpler if-else structure for validation. The `sendNotificationMsgToEdge` call did not include `edgeSynchronizationManager.getEdgeId().get()` as a parameter.

**Parent 2** had renamed the method to `isValidSaveEntityEventForEdgeProcessing` — a more descriptive name — and restructured its body to use a `switch` on `EntityType` with `entity` and `oldEntity` extracted as local variables. P2 had also added `edgeSynchronizationManager.getEdgeId().get()` as a new argument to `sendNotificationMsgToEdge` calls.

---

## Interpretation

This case involves two refactorings performed in P2 that conflicted with P1:

### 1. Rename_Method

P2 renamed `isValidEdgeEventEntity` → `isValidSaveEntityEventForEdgeProcessing`. The `--` line removes the old name and the `++` line introduces the new name, both at the method declaration and at its call sites. This is a direct **Rename_Method** with clear `--`/`++` merge effort evidence.

### 2. Extract_Variable + body restructuring

The `++` lines `Object entity = event.getEntity()` and `Object oldEntity = event.getOldEntity()` represent an **Extract_Variable** refactoring: P2 extracted repeated `event.getEntity()` and `event.getOldEntity()` calls into named local variables used throughout the switch body. P1 still had the inlined calls. The merge had to produce `++` lines to apply this extraction in the context of the merged method body.

The combination of **Rename_Method** and **Extract_Variable** in the same method, both visible as `++`/`--` lines, makes this a defensible and well-structured case.
