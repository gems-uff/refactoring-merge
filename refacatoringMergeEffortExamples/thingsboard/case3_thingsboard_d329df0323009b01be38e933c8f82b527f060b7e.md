# Case 1 — Project: thingsboard — Merge commit SHA1: d329df0323009b01be38e933c8f82b527f060b7e

---

## Modified file(s):
`application/src/main/java/org/thingsboard/server/cluster/DefaultTbClusterService.java`

---

## Class(es) modified in the merge:
`DefaultTbClusterService`

---

## Merge effort lines in the combined diff

```diff
-- public void onResourceChange(TbResource resource, TbQueueCallback callback) {
++ public void onResourceChange(TbResourceInfo resource, TbQueueCallback callback) {

-- public void onResourceDeleted(TbResource resource, TbQueueCallback callback) {
++ public void onResourceDeleted(TbResourceInfo resource, TbQueueCallback callback) {
```

---

## Relevant final code in the merge

```java
@Override
public void onResourceChange(TbResourceInfo resource, TbQueueCallback callback) {
    TenantId tenantId = resource.getTenantId();
    log.trace("[{}][{}][{}] Processing change resource", tenantId, resource.getResourceType(), resource.getResourceKey());
    // ...
}

@Override
public void onResourceDeleted(TbResourceInfo resource, TbQueueCallback callback) {
    log.trace("[{}] Processing delete resource", resource);
    // ...
}
```

---

## Number of merge-effort lines (`++` and `--`) associated with the refactoring types under analysis:
**4 lines**

---

## What each side had

**Parent 1** had `onResourceChange(TbResource resource, TbQueueCallback callback)` and `onResourceDeleted(TbResource resource, TbQueueCallback callback)` — using the full `TbResource` entity type as the parameter.

**Parent 2** had performed a **Change_Parameter_Type** refactoring: both method signatures were updated to use `TbResourceInfo` instead of `TbResource`. `TbResourceInfo` is the leaner parent/info class of `TbResource`, reflecting a design decision to work with resource metadata rather than the full resource entity at this layer.

---

## Interpretation

P2 performed a **Change_Parameter_Type** refactoring on two methods in `DefaultTbClusterService`, changing the parameter type from `TbResource` to `TbResourceInfo` for both `onResourceChange` and `onResourceDeleted`. Since P1 still had the `TbResource` signatures, the merge had to produce `--`/`++` lines to resolve the conflict and establish `TbResourceInfo` as the canonical parameter type.

This is a direct and defensible **Change_Parameter_Type** case: the type narrows from a subclass (`TbResource`) to its parent info class (`TbResourceInfo`), applied consistently across two related methods. The `++` lines confirm the merge applied P2's type change.
