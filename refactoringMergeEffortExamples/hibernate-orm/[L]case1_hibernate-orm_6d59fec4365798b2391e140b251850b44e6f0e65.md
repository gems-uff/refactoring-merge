# Case 1 — Project: hibernate-orm — Merge commit SHA1: 6d59fec4365798b2391e140b251850b44e6f0e65

## Modified file(s):
- `hibernate-core/src/main/java/org/hibernate/persister/entity/Loadable.java`
- `hibernate-core/src/main/java/org/hibernate/persister/entity/AbstractEntityPersister.java`

## Class(es) modified in the merge:
- `Loadable` (interface)
- `AbstractEntityPersister`

## Merge effort lines in the combined diff

### Loadable.java — `Change_Parameter_Type`: `Serializable id` → `Object id` in new `hydrate()` default overload

```diff
+ 	/**
+ 	 * Retrieve property values from one row of a result set
+ 	 */
+ 	default Object[] hydrate(
+ 			ResultSet rs,
 -			Serializable id,
++			Object id,
+ 			Object object,
+ 			Loadable rootLoadable,
+ 			String[][] suffixedPropertyColumns,
+ 			boolean forceEager,
+ 			SharedSessionContractImplementor session) throws SQLException, HibernateException {
+ 		return hydrate( rs, id, object, rootLoadable, suffixedPropertyColumns, forceEager, null, session );
+ 	}
```

### AbstractEntityPersister.java — `Change_Parameter_Type`: `Serializable id` → `Object id` in `hydrate()` implementation

```diff
+ 	@Override
 	public Object[] hydrate(
 			final ResultSet rs,
 -			final Serializable id,
 +			final Object id,
 			final Object object,
 			final Loadable rootLoadable,
 			final String[][] suffixedPropertyColumns,
```

## Relevant final code in the merge

```java
// Loadable.java — default 7-arg overload
default Object[] hydrate(
        ResultSet rs,
        Object id,            // was Serializable
        Object object,
        Loadable rootLoadable,
        String[][] suffixedPropertyColumns,
        boolean forceEager,
        SharedSessionContractImplementor session) throws SQLException, HibernateException {
    return hydrate( rs, id, object, rootLoadable, suffixedPropertyColumns, forceEager, null, session );
}

// AbstractEntityPersister.java
@Override
public Object[] hydrate(
        final ResultSet rs,
        final Object id,       // was Serializable
        final Object object,
        final Loadable rootLoadable,
        final String[][] suffixedPropertyColumns,
        ...
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
**3 lines** (2 `++` for the new `Object id` parameter lines, 1 `++` for `@Override` added to `AbstractEntityPersister`)

## What each side had

**Parent 1 (P1)** had the `Loadable.hydrate()` new default 7-argument overload already using `Object id` as the parameter type, and `AbstractEntityPersister.hydrate()` also using `Object id`.

**Parent 2 (P2)** still had both signatures using `Serializable id` as the parameter type — the old, more restrictive type that predates the change. P2 did not have the new 7-arg default method at all in the `Loadable` interface.

## Interpretation

This is a clear **Change_Parameter_Type** refactoring. P1 changed the `id` parameter type in the `hydrate()` method from `Serializable` to `Object` in both the `Loadable` interface and its concrete implementation `AbstractEntityPersister`. The `--` lines show `Serializable id` in P2's version of the method signatures, while the `++` lines confirm the merge adopted `Object id` (from P1).

The evidence is unambiguous: the combined diff shows `" -  final Serializable id,"` and `" +  final Object id,"` as the two parent contributions, with `"++"` for `Object id` in the new default overload of the `Loadable` interface — confirming that P1's more general type (`Object`) was the chosen resolution in both files.

## Complete diff

```diff
diff --cc hibernate-core/src/main/java/org/hibernate/persister/entity/AbstractEntityPersister.java
index 6f3c82e3984,61a2b609ce6..20c2ac085b3
--- a/hibernate-core/src/main/java/org/hibernate/persister/entity/AbstractEntityPersister.java
+++ b/hibernate-core/src/main/java/org/hibernate/persister/entity/AbstractEntityPersister.java
@@@ -3298,9 -3127,10 +3298,10 @@@
 	 * without resolving associations or collections. Question: should
 	 * this really be here, or should it be sent back to Loader?
 	 */
+ 	@Override
 	public Object[] hydrate(
 			final ResultSet rs,
 -			final Serializable id,
 +			final Object id,
 			final Object object,
 			final Loadable rootLoadable,
 			final String[][] suffixedPropertyColumns,

diff --cc hibernate-core/src/main/java/org/hibernate/persister/entity/Loadable.java
index 4e9f7386d1d,5a0e1054c87..82a286d072f
--- a/hibernate-core/src/main/java/org/hibernate/persister/entity/Loadable.java
+++ b/hibernate-core/src/main/java/org/hibernate/persister/entity/Loadable.java
@@@ -79,6 -80,20 +79,20 @@@
 	boolean hasRowId();
 	
+ 	/**
+ 	 * Retrieve property values from one row of a result set
+ 	 */
+ 	default Object[] hydrate(
+ 			ResultSet rs,
 -			Serializable id,
++			Object id,
+ 			Object object,
+ 			Loadable rootLoadable,
+ 			String[][] suffixedPropertyColumns,
+ 			boolean forceEager,
+ 			SharedSessionContractImplementor session) throws SQLException, HibernateException {
+ 		return hydrate( rs, id, object, rootLoadable, suffixedPropertyColumns, forceEager, null, session );
+ 	}

 	/**
 	 * Retrieve property values from one row of a result set
 	 */

diff --cc hibernate-core/src/test/java/org/hibernate/orm/test/schemaupdate/PostgreSQLMultipleSchemaSequenceTest.java
index 5edb0613434,ad5fc1afebc..2642cabc149
--- a/hibernate-core/src/test/java/org/hibernate/orm/test/schemaupdate/PostgreSQLMultipleSchemaSequenceTest.java
+++ b/hibernate-core/src/test/java/org/hibernate/orm/test/schemaupdate/PostgreSQLMultipleSchemaSequenceTest.java
@@@ -156,7 -156,7 +156,7 @@@
 			assertEquals( 2 ,
 					      sqlLines
 					      .stream()
- 					      .filter( s -> s.equalsIgnoreCase( "create sequence SEQ_TEST start with 1 increment by 1" ) )
 -					      .filter( s -> s.equalsIgnoreCase( "create sequence SEQ_TEST start 1 increment 1;" ) )
++					      .filter( s -> s.equalsIgnoreCase( "create sequence SEQ_TEST start with 1 increment by 1;" ) )
 					      .count()
 			);

diff --cc hibernate-graalvm/src/main/java/org/hibernate/graalvm/internal/QueryParsingSupport.java
index 00000000000,28b1833e77d..106d3b4b213
mode 000000,100644..100644
--- a/hibernate-graalvm/src/main/java/org/hibernate/graalvm/internal/QueryParsingSupport.java
+++ b/hibernate-graalvm/src/main/java/org/hibernate/graalvm/internal/QueryParsingSupport.java
@@@ -1,0 -1,154 +1,154 @@@
+ [new file — P2 only, all + lines, no conflict — class references to old HQL parser packages
+  replaced with ++ lines pointing to new org.hibernate.grammars.hql package]
++		Class<?> lexerClazz = access.findClassByName("org.hibernate.grammars.hql.HqlLexer");
++		Class<?> parserClazz = access.findClassByName("org.hibernate.grammars.hql.HqlParser");
```
