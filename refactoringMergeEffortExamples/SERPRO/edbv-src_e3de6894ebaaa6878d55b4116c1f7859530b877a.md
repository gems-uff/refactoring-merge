# Project: edbv-src — Merge commit SHA1: e3de6894ebaaa6878d55b4116c1f7859530b877a

## Modified file(s)
- `aplicacao/edbv-parent/edbv-carga-api/src/main/java/br/gov/serpro/edbv/carga/voos/servlet/QueuesInfo.java`

## Class(es) affected by the merge effort
- `QueuesInfo` (formerly `WMQQueuesInfo`)

## Merge effort lines in the combined diff

**Class declaration — Parent 1 still named the class `WMQQueuesInfo`; Parent 2 had renamed it to `QueuesInfo`. The merge fabricated the resolved declaration line carrying the new name:**

```diff
-  public class WMQQueuesInfo
 -public class QueuesInfo extends HttpServlet {
++public class QueuesInfo
 +		extends HttpServlet {
```

**Constructor — same rename reflected in the constructor name; the merge fabricated the resolved constructor signature with the new name:**

```diff
-      public WMQQueuesInfo() {
++    public QueuesInfo() {
 +		super();
 +		LOGGER.info(".............. INIT SERVLET QUEUES INFO ................");
```

## Relevant final code in the merge

```java
@WebServlet("/queuesInfo")
public class QueuesInfo
		extends HttpServlet {

    private static final long serialVersionUID = 1L;
    // ...
    public QueuesInfo() {
		super();
		LOGGER.info(".............. INIT SERVLET QUEUES INFO ................");
		// ...
	}
    // ...
}
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis
- **2 `++` lines** directly associated with **Rename_Class**:
  - `public class QueuesInfo`
  - `public QueuesInfo() {`
- **0 `--` lines** associated with this refactoring.

(The remaining ~61 `++` lines in this large diff are excluded. The dominant pattern across ~10 files — adding `import ...config.EdbvConfig;`, an `@Inject private EdbvConfig edbvConfig;` field, and replacing the ambient lookup with `this.edbvConfig.getAmbiente()` — is a refactoring fabricated **by the merge itself** to reconcile two divergent parent approaches (Parent 1 used `EdbvUtil.getAmbiente()`, Parent 2 used `AmbienteEnum.valueOfString(System.getProperty("edbv.ambiente"))`); neither parent contained `EdbvConfig`, so per rule 2 it is merge-introduced and not reportable. Other `++` lines are unresolved conflict markers, `pom.xml`/`.conf` config, and whitespace.)

## What each side had

- **Parent 1** held the class under the name `WMQQueuesInfo` (note: the file path was already `QueuesInfo.java`, so Parent 1's class name did not match its file name):
  ```java
  // Parent 1 — file QueuesInfo.java
  @WebServlet("/queuesInfo")
  public class WMQQueuesInfo extends HttpServlet {
      // ...
      public WMQQueuesInfo() {
          // ...
      }
  }
  ```
  In the combined diff, Parent 1's class line and constructor line appear as `- ` (Parent-1-discarded) lines.

- **Parent 2 contains the evidence of the refactoring.** It had already renamed the class to `QueuesInfo`, matching the file name:
  ```java
  // Parent 2 — file QueuesInfo.java
  @WebServlet("/queuesInfo")
  public class QueuesInfo extends HttpServlet {
      // ...
  }
  ```
  Parent 2's class declaration appears as a ` -` (Parent-2-discarded, due to a formatting difference on `extends`) line, and the merge adopted its name.

## Interpretation

The refactoring evidenced by the combined diff is **Rename_Class** (`WMQQueuesInfo` → `QueuesInfo`), introduced **in Parent 2**.

The conflict is a class-identity conflict at a single file path: both parents have a `QueuesInfo.java`, but Parent 1's file still declares `class WMQQueuesInfo` while Parent 2's declares `class QueuesInfo`. The evidence is parent-side:

- `- public class WMQQueuesInfo` — Parent-1-discarded class declaration with the old name.
- ` -public class QueuesInfo extends HttpServlet {` — Parent-2-discarded class declaration with the new name (discarded only because of an `extends` line-break formatting difference).
- `- public WMQQueuesInfo() {` — Parent-1-discarded constructor with the old name.

The two `++` lines (`public class QueuesInfo` and `public QueuesInfo() {`) are the **merge effort**: because the two parents disagreed on the class/constructor identifier on those exact lines, the merge could inherit neither verbatim and fabricated the resolved declarations adopting Parent 2's new name `QueuesInfo`. The new identifier is carried directly on the `++` lines.

The refactoring was **not** introduced by the merge itself: Parent 1's old-name lines (`WMQQueuesInfo`) are present as `- ` discarded lines and Parent 2's new-name declaration is present as a ` -` discarded line — both parent-side artifacts predating the merge. The merge only selected Parent 2's renamed identifier and re-emitted the conflicting declaration lines.

This case is distinguished from the merge-introduced `EdbvConfig` injection that dominates the rest of this commit: the `EdbvConfig` abstraction did not exist in either parent and was created during the merge (excluded by rule 2), whereas the `QueuesInfo` name already existed in Parent 2 before the merge. Note also that the two parents' class *bodies* diverged substantially (Parent 1's implementation differs from Parent 2's WMQ/PCFMessage-based implementation); this case is scoped specifically to the class/constructor naming conflict that the `++` lines resolve, which is the part attributable to the Rename_Class refactoring.
