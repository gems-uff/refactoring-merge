# Project: edbv-src_3fdab376e47303766c387c2299759f7f43394d8c

## Modified file(s):
`pucomex-ccta-ext/src/main/java/br/gov/serpro/pucomex/ccta/consultadetalhada/service/ConsultaDetalhadaConhecimentoService.java`

## Class(es) affected by the merge effort:
`ConsultaDetalhadaConhecimentoService`

## Merge effort lines in the combined diff

```diff
 +      if ((parametrosConsulta.getDataEmissao() != null) && !"".equals(parametrosConsulta.getDataEmissao().trim())) {
 +          LocalDate dataEmissao = DateUtil
 +                  .getLocalDate(DateUtil.getZonedDate(parametrosConsulta.getDataEmissao(), false));
-           query.setDataVigencia(dataEmissao);
++          query.setDataExclusividade(dataEmissao);
 +      }
```

## Relevant final code in the merge

```java
BuscarConhecimentoUnicoQuery query = BuscarConhecimentoUnicoQuery.builder()
        .numero(parametrosConsulta.getIdentificacaoConhecimento())
        .cnpjRepresentado(parametrosConsulta.getCnpjResponsavel()).build();

if ((parametrosConsulta.getDataEmissao() != null) && !"".equals(parametrosConsulta.getDataEmissao().trim())) {
    LocalDate dataEmissao = DateUtil
            .getLocalDate(DateUtil.getZonedDate(parametrosConsulta.getDataEmissao(), false));
    query.setDataExclusividade(dataEmissao);   // ++ merged result
}
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
**1 line** (`++` for `query.setDataExclusividade(dataEmissao)`)

## What each side had

**Parent 1** had (evidenced by `" -"` line):
```java
- query.setDataVigencia(dataEmissao);
```

**Parent 2** had (the new name — inferred from `++` line and the fact that P2 contributed the surrounding block of `" +"` context lines):
```java
// P2 was the branch that introduced this new method body, and used the updated setter name:
query.setDataExclusividade(dataEmissao);
```

The `" -"` line shows that P1 still used `setDataVigencia`, while the `++` merge resolution uses `setDataExclusividade`. This means P2 had already renamed the attribute/setter on `BuscarConhecimentoUnicoQuery`, while P1's new code for `ConsultaDetalhadaConhecimentoService` (which P1 contributed — the `" +"` block of new method body) still referenced the old name `setDataVigencia`.

## Interpretation

**Refactoring type**: **Rename_Attribute** (the `dataVigencia` field on `BuscarConhecimentoUnicoQuery` was renamed to `dataExclusividade`, along with its setter).

Parent 2 renamed the attribute `dataVigencia` → `dataExclusividade` in `BuscarConhecimentoUnicoQuery` (and its corresponding setter). Parent 1 added a new method body to `ConsultaDetalhadaConhecimentoService` that used the old setter name `setDataVigencia`. The conflict arose because P1's new code referenced the old attribute name that P2 had renamed.

The `++` line is the merge effort that resolves this: it uses the new renamed setter `setDataExclusividade`, adapting P1's new code to P2's renamed attribute. The refactoring (rename) was introduced in Parent 2, not by the merge itself — the merge only propagated the rename into P1's new code.

This case is well-supported: the single `" -"` / `"++"` pair is precisely at the setter call, and the semantic difference (`dataVigencia` vs `dataExclusividade`) clearly indicates an intentional rename in the parent branch, not merely a formatting change.
