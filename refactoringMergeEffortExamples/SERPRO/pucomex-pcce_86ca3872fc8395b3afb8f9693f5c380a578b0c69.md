# Project: pucomex-pcce_86ca3872fc8395b3afb8f9693f5c380a578b0c69

## Modified file(s):
- `pucomex-ccta-commons/src/main/java/br/gov/serpro/pucomex/ccta/commons/application/mapper/QueryCargaMapper.java` (merge effort — call site)
- `pucomex-ccta-commons/src/main/java/br/gov/serpro/pucomex/ccta/commons/domain/Conhecimento.java` (supporting evidence — the renamed getter)

## Class(es) affected by the merge effort:
- `QueryCargaMapper` (stale call site updated in the merge)
- `Conhecimento` (domain class whose getter was renamed in one parent)

## Merge effort lines in the combined diff

```diff
@@@ QueryCargaMapper.java @@@
- 		Optional.ofNullable(conh.getConsignatario()) //
++		Optional.ofNullable(conh.getParteInteressadaConsignatario()) //
  +				.map(ParteInteressada::getNome) //
  +				.ifPresent(builder::consignatarioNomeConhecimento);
```

Supporting evidence — the getter rename in the domain class (single-column `- `/`+ `,
i.e. inherited from Parent 2 without merge effort):

```diff
@@@ Conhecimento.java @@@
- 	public ParteInteressada getConsignatario() {
+ 	public ParteInteressada getParteInteressadaConsignatario() {
```

## Relevant final code in the merge

```java
Optional.ofNullable(conh.getParteInteressadaConsignatario())
        .map(ParteInteressada::getNome)
        .ifPresent(builder::consignatarioNomeConhecimento);
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
1 line (`++`) — the updated call site `conh.getParteInteressadaConsignatario()`.

## What each side had
- **Parent 1** still called the **old** getter name `conh.getConsignatario()` (the
  `- ` discarded-from-Parent-1 line in `QueryCargaMapper.java`).
- **Parent 2** is where the **refactoring lives**: it renamed the domain getter
  `getConsignatario()` → `getParteInteressadaConsignatario()` in `Conhecimento.java`
  (the `+ ` line inheriting the new signature, `- ` dropping the old one) and its own
  mapper code already referenced the new name (`+ ` context lines such as
  `Optional.ofNullable(conh.getParteInteressadaConsignatario())...`).

## Interpretation
The refactoring is a **Rename_Method**: the accessor `getConsignatario()` on
`Conhecimento` was renamed to `getParteInteressadaConsignatario()` in **Parent 2**.
`QueryCargaMapper` was edited on both branches; Parent 1's version still invoked the old
getter, so when the branches were merged the call site referenced a method that no longer
existed under that name. The merge resolved this by emitting the `++` line that retargets
the call to the renamed getter `getParteInteressadaConsignatario()`. The renamed method
definition inherited from Parent 2 confirms the rename pre-dates the merge, so the single
`++` line is genuine conflict-resolution effort caused by a parent refactoring rather than
a rename introduced by the merge itself.
