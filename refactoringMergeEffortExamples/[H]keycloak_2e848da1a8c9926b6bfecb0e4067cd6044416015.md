# Project: keycloak — Merge commit SHA1: 2e848da1a8c9926b6bfecb0e4067cd6044416015

## Modified file(s):
- `model/api/src/main/java/org/keycloak/models/utils/DefaultAuthenticationFlows.java`
- `model/api/src/main/java/org/keycloak/models/utils/RepresentationToModel.java`
- `services/src/main/java/org/keycloak/services/resources/admin/UserFederationProvidersResource.java`

## Class(es) modified in the merge:
- `DefaultAuthenticationFlows`
- `RepresentationToModel`
- `UserFederationProvidersResource`

## Merge effort lines in the combined diff

### 1. `DefaultAuthenticationFlows` — `migrateFlows` extracted and `browserFlow` parameterized (P2)

```diff
+ public static void browserFlow(RealmModel realm) {
++        browserFlow(realm, false);
++    }
++
++    private static boolean hasCredentialType(RealmModel realm, String type) {
++        for (RequiredCredentialModel requiredCredentialModel : realm.getRequiredCredentials()) {
++            if (type.equals(requiredCredentialModel.getType())) {
++                return true;
++            }
++
++        }
++        return false;
++    }
++
++    public static void browserFlow(RealmModel realm, boolean migrate) {
+         AuthenticationFlowModel browser = new AuthenticationFlowModel();
...
++        if (migrate && hasCredentialType(realm, RequiredCredentialModel.KERBEROS.getType())) {
++            execution.setRequirement(AuthenticationExecutionModel.Requirement.ALTERNATIVE);
++
++        }
...
++        if (migrate && hasCredentialType(realm, RequiredCredentialModel.TOTP.getType())) {
++            execution.setRequirement(AuthenticationExecutionModel.Requirement.REQUIRED);
++
++        }
++
```

### 2. `RepresentationToModel.importAuthenticationFlows` — call-site reconciliation (merge effort)

```diff
+         if (rep.getAuthenticationFlows() == null) {
+             // assume this is an old version being imported
-            DefaultAuthenticationFlows.addFlows(newRealm);
++            DefaultAuthenticationFlows.migrateFlows(newRealm);
+         } else {
```

### 3. `RepresentationToModel` — `addRequiredCredential` helper introduced by P1, inlined by merge

```diff
-    public static void addRequiredCredential(RealmModel newRealm, String requiredCred) {
-        newRealm.addRequiredCredential(requiredCred);
-    }
-
+ // todo remove this stuff as its all deprecated
+         if (rep.getRequiredCredentials() != null) {
+             for (String requiredCred : rep.getRequiredCredentials()) {
-                addRequiredCredential(newRealm, requiredCred);
++                newRealm.addRequiredCredential(requiredCred);
+             }
+         } else {
-            addRequiredCredential(newRealm, CredentialRepresentation.PASSWORD);
++            newRealm.addRequiredCredential(CredentialRepresentation.PASSWORD);
+         }
```

### 4. `UserFederationProvidersResource` — `checkKerberosCredential` moved from `KeycloakModelUtils` (P2 Move_Method)

```diff
++    public static boolean checkKerberosCredential(KeycloakSession session, RealmModel realm, UserFederationProviderModel model) {
++        String allowKerberosCfg = model.getConfig().get(KerberosConstants.ALLOW_KERBEROS_AUTHENTICATION);
++        if (Boolean.valueOf(allowKerberosCfg)) {
++            CredentialHelper.setAlternativeCredential(session, CredentialRepresentation.KERBEROS, realm);
++            return true;
++        }
++
++        return false;
++    }
...
-        boolean kerberosCredsAdded = KeycloakModelUtils.checkKerberosCredential(realm, model);
++        boolean kerberosCredsAdded = checkKerberosCredential(session, realm, model);
```

## Relevant final code in the merge

```java
// DefaultAuthenticationFlows.java — migrateFlows (new method from P2)
public static void migrateFlows(RealmModel realm) {
    browserFlow(realm, true);
    if (realm.getFlowByAlias(REGISTRATION_FLOW) == null) registrationFlow(realm);
}

public static void browserFlow(RealmModel realm) {
    browserFlow(realm, false);
}

public static void browserFlow(RealmModel realm, boolean migrate) {
    // ... uses hasCredentialType for KERBEROS and TOTP conditionals
}

// RepresentationToModel.java — inline of addRequiredCredential
newRealm.addRequiredCredential(requiredCred);   // was: addRequiredCredential(newRealm, requiredCred)

// RepresentationToModel.importAuthenticationFlows — call updated
DefaultAuthenticationFlows.migrateFlows(newRealm);   // was: addFlows(newRealm)

// UserFederationProvidersResource — call updated to local method
boolean kerberosCredsAdded = checkKerberosCredential(session, realm, model);
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
**36 lines** (++ and -- combined across all three files)

## What each side had

**Parent 1 (P1):**
- `DefaultAuthenticationFlows.browserFlow(RealmModel)` — non-parameterized, single method.
- `RepresentationToModel` contained `addRequiredCredential(RealmModel, String)` as a public static helper method used internally in `importRealm`.
- `importAuthenticationFlows` called `DefaultAuthenticationFlows.addFlows(newRealm)` for old version import.

**Parent 2 (P2):**
- Introduced `DefaultAuthenticationFlows.migrateFlows(RealmModel)` and refactored `browserFlow` into a two-arg form (`browserFlow(RealmModel, boolean migrate)`) with a private helper `hasCredentialType`.
- Moved `checkKerberosCredential` from `KeycloakModelUtils` into `UserFederationProvidersResource`, adding a `session` parameter and calling `CredentialHelper.setAlternativeCredential`.
- `importAuthenticationFlows` now calls `migrateFlows` instead of `addFlows`.

## Interpretation

Three distinct refactoring types are evidenced:

1. **Extract_Method / Change_Parameter_Type (`browserFlow`)**: P2 extracted a private helper `hasCredentialType` and added a `boolean migrate` parameter to `browserFlow`, splitting the single-signature method into two. The `++` lines for `browserFlow(realm, false)`, the new `hasCredentialType` method body, and the conditional blocks inside `browserFlow(RealmModel, boolean)` are all merge-effort lines that reconcile P2's structural change with P1's unchanged call sites.

2. **Rename_Method (`addRequiredCredential`)**: P1 had introduced `addRequiredCredential` as a one-line wrapper. 

3. **Move_Method (`checkKerberosCredential`)**: P2 moved `checkKerberosCredential` from `KeycloakModelUtils` into `UserFederationProvidersResource`, adding a `KeycloakSession` parameter. The merge-effort `++` lines add the full method body inside the resource class, and the `--`/`++` lines at the call site switch from `KeycloakModelUtils.checkKerberosCredential(realm, model)` to `checkKerberosCredential(session, realm, model)`.

All cases are well-supported: the `- ` and `+ ` lines clearly show what each parent contributed, and the `++`/`--` lines represent exactly the work done to reconcile the two sides.

## Complete diff

<details>
<summary>Click to expand full combined diff</summary>

See uploaded file: `keycloak_2e848da1a8c9926b6bfecb0e4067cd6044416015.diff`

</details>
