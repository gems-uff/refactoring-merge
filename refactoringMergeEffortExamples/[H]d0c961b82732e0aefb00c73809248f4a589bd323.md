# Project: RxJava — Merge commit SHA1: d0c961b82732e0aefb00c73809248f4a589bd323

## Modified file(s):
- `rxjava-core/src/main/java/rx/operators/OperationObserveOn.java`
- `rxjava-core/src/main/java/rx/operators/OperationSubscribeOn.java`
- `rxjava-core/src/main/java/rx/operators/OperationTake.java`
- `rxjava-core/src/main/java/rx/operators/OperatorTester.java`
- `rxjava-core/src/main/java/rx/subjects/DefaultSubject.java`

## Class(es) modified in the merge:
- `Tester` → `OperatorTester` (renamed)
- `PublishSubject` → `DefaultSubject` (renamed, within `DefaultSubject.java`)

---

## Case 3a — Rename_Class: `Tester` → `OperatorTester`

### Merge effort lines in the combined diff

```diff
// OperationObserveOn.java
-             Scheduler scheduler = spy(Tester.UnitTest.forwardingScheduler(Schedulers.immediate()));
++            Scheduler scheduler = spy(OperatorTester.UnitTest.forwardingScheduler(Schedulers.immediate()));

// OperationSubscribeOn.java
-             Scheduler scheduler = spy(Tester.UnitTest.forwardingScheduler(Schedulers.immediate()));
++            Scheduler scheduler = spy(OperatorTester.UnitTest.forwardingScheduler(Schedulers.immediate()));

// OperationTake.java
- import static rx.operators.Tester.UnitTest.*;
++import static rx.operators.OperatorTester.UnitTest.*;

// OperatorTester.java (class declaration)
- /* package */class Tester {
++/* package */class OperatorTester {

-     private Tester() {
++    private OperatorTester() {
```

### Relevant final code in the merge

```java
// OperatorTester.java
/* package */class OperatorTester {
    private OperatorTester() { }
    public static class UnitTest { ... }
}

// OperationTake.java
import static rx.operators.OperatorTester.UnitTest.*;

// OperationObserveOn.java / OperationSubscribeOn.java
Scheduler scheduler = spy(OperatorTester.UnitTest.forwardingScheduler(Schedulers.immediate()));
```

### Number of merge-effort lines (++ and --) associated with this refactoring:
7 lines (`++`)

### What each side had

**Parent 1** had the utility class named `Tester` in `rx.operators`, and call sites in `OperationObserveOn`, `OperationSubscribeOn`, and `OperationTake` referred to `Tester.UnitTest`.

**Parent 2** renamed the class to `OperatorTester` (also renaming the private constructor), while also modifying the same file with other changes. Parent 1's files still referenced the old name `Tester`.

### Interpretation

This is a clear **Rename_Class** refactoring: `Tester` → `OperatorTester` was introduced in Parent 2's branch. The merge had to update all three call sites in other files (`OperationObserveOn`, `OperationSubscribeOn`, `OperationTake`) and the class declaration itself. The `++` lines at all four locations confirm that the merger rewrote every occurrence of the old name to match the rename from P2, resolving the conflict with P1 which still used `Tester`.

---

## Case 3b — Rename_Class: `PublishSubject` → `DefaultSubject`

### Merge effort lines in the combined diff

```diff
// DefaultSubject.java - test methods (merged into the file)
 -            PublishSubject<Object> subject = PublishSubject.create();
++            DefaultSubject<Object> subject = DefaultSubject.create();

 -            PublishSubject<Object> subject = PublishSubject.create();
++            DefaultSubject<Object> subject = DefaultSubject.create();

 -            PublishSubject<Object> subject = PublishSubject.create();
++            DefaultSubject<Object> subject = DefaultSubject.create();

 -            PublishSubject<Object> subject = PublishSubject.create();
++            DefaultSubject<Object> subject = DefaultSubject.create();

// (and in Func0/Action1 generic parameters in UnsubscribeTester calls)
++            UnsubscribeTester.test(new Func0<DefaultSubject<Object>>() { ... }
++                public DefaultSubject<Object> call() { return DefaultSubject.create(); }
```

Also, the class declaration itself:
```diff
 -public class PublishSubject<T> extends Subject<T, T> {
 -    public static <T> PublishSubject<T> create() {
 +public class DefaultSubject<T> extends Subject<T, T> {
 +    public static <T> DefaultSubject<T> create() {
```
(These are `" +"` / `" -"` P2 context lines, with the test-method updates being the `++` merge effort.)

### Relevant final code in the merge

```java
public class DefaultSubject<T> extends Subject<T, T> {
    public static <T> DefaultSubject<T> create() { ... }

    // test methods inside UnitTest
    public void testCompleted() {
        DefaultSubject<Object> subject = DefaultSubject.create();
        ...
    }
}
```

### Number of merge-effort lines (++ and --) associated with this refactoring:
~15 lines (`++`)

### What each side had

**Parent 1** had `PublishSubject` in `rx.subjects` as the concrete subject class name, and the unit-test methods inside `DefaultSubject.java` (which was being merged from P1 where the file already partially existed) still referred to `PublishSubject.create()`.

**Parent 2** renamed the class to `DefaultSubject` and its factory method to `DefaultSubject.create()`, while also migrating the file. The existing test-method bodies in P1 still used `PublishSubject`.

### Interpretation

**Rename_Class** (`PublishSubject` → `DefaultSubject`): introduced in Parent 2. The merge effort `++` lines update each `PublishSubject.create()` reference in the unit-test methods to `DefaultSubject.create()` to reconcile P1's old references with P2's renamed class. The ` -` lines (P1 context) show exactly what was replaced.

## Complete diff

```diff
diff --cc rxjava-core/src/main/java/rx/operators/OperationObserveOn.java
...
-             Scheduler scheduler = spy(Tester.UnitTest.forwardingScheduler(Schedulers.immediate()));
++            Scheduler scheduler = spy(OperatorTester.UnitTest.forwardingScheduler(Schedulers.immediate()));
...

diff --cc rxjava-core/src/main/java/rx/operators/OperationSubscribeOn.java
...
-             Scheduler scheduler = spy(Tester.UnitTest.forwardingScheduler(Schedulers.immediate()));
++            Scheduler scheduler = spy(OperatorTester.UnitTest.forwardingScheduler(Schedulers.immediate()));
...

diff --cc rxjava-core/src/main/java/rx/operators/OperationTake.java
- import static rx.operators.Tester.UnitTest.*;
++import static rx.operators.OperatorTester.UnitTest.*;
...

diff --cc rxjava-core/src/main/java/rx/operators/OperatorTester.java
- /* package */class Tester {
++/* package */class OperatorTester {
-     private Tester() {
++    private OperatorTester() {
...

diff --cc rxjava-core/src/main/java/rx/subjects/DefaultSubject.java
 -            PublishSubject<Object> subject = PublishSubject.create();
++            DefaultSubject<Object> subject = DefaultSubject.create();
[repeated for 4 test methods]
...
```
