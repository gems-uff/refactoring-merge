# Case 2 — Project: elasticsearch — Merge commit SHA1: 10c5ac635326ef82f723342309765f689c45a06f

## Modified file(s):
- `src/main/java/org/elasticsearch/watcher/support/http/HttpRequest.java`
- `src/main/java/org/elasticsearch/watcher/support/http/HttpRequestTemplate.java`
- `src/test/java/org/elasticsearch/watcher/actions/email/EmailActionTests.java`
- `src/test/java/org/elasticsearch/watcher/test/WatcherTestUtils.java`

## Class(es) modified in the merge:
- `HttpRequest`
- `HttpRequestTemplate`
- `EmailActionTests`
- `WatcherTestUtils`

## Merge effort lines in the combined diff

```diff
# HttpRequest.java — parseTimeValue signature conflict
                 } else if (Field.CONNECTION_TIMEOUT.match(currentFieldName)) {
                     try {
 -                        builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                        builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.CONNECTION_TIMEOUT.toString()));
                     } catch (WatcherDateTimeUtils.ParseException pe) {

                 } else if (Field.READ_TIMEOUT.match(currentFieldName)) {
                     try {
 -                        builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                        builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.READ_TIMEOUT.toString()));

# HttpRequestTemplate.java — same parseTimeValue conflict
                 } else if (Field.CONNECTION_TIMEOUT.match(currentFieldName)) {
                     try {
 -                        builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                        builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.CONNECTION_TIMEOUT.toString()));

                 } else if (Field.READ_TIMEOUT.match(currentFieldName)) {
                     try {
 -                        builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                        builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.READ_TIMEOUT.toString()));

# EmailActionTests.java — constructor parameter conflict
-         ExecutableEmailAction executable = new EmailActionFactory(Settings.EMPTY, emailService, engine)
 -        ExecutableEmailAction executable = new EmailActionFactory(ImmutableSettings.EMPTY, emailService, engine, htmlSanitizer)
++        ExecutableEmailAction executable = new EmailActionFactory(Settings.EMPTY, emailService, engine, htmlSanitizer)

-         ExecutableEmailAction parsed = new EmailActionFactory(Settings.EMPTY, service, engine)
 -        ExecutableEmailAction parsed = new EmailActionFactory(ImmutableSettings.EMPTY, service, engine, htmlSanitizer)
++        ExecutableEmailAction parsed = new EmailActionFactory(Settings.EMPTY, service, engine, htmlSanitizer)

-         new EmailActionFactory(Settings.EMPTY, emailService, engine)
 -        new EmailActionFactory(ImmutableSettings.EMPTY, emailService, engine, htmlSanitizer)
++        new EmailActionFactory(Settings.EMPTY, emailService, engine, htmlSanitizer)

# WatcherTestUtils.java — constructor parameter conflict
-         ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine);
 -        ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine, new HtmlSanitizer(ImmutableSettings.EMPTY));
++        ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine, new HtmlSanitizer(Settings.EMPTY));
```

## Relevant final code in the merge

```java
// HttpRequest.java
builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.CONNECTION_TIMEOUT.toString()));
builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.READ_TIMEOUT.toString()));

// EmailActionTests.java
ExecutableEmailAction executable = new EmailActionFactory(Settings.EMPTY, emailService, engine, htmlSanitizer)
        .parseExecutable(randomAsciiOfLength(8), randomAsciiOfLength(3), parser);

// WatcherTestUtils.java
ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine, new HtmlSanitizer(Settings.EMPTY));
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
14 lines (7 `--` lines and 7 `++` lines)

## What each side had

**Parent 1** had `EmailActionFactory` and `ExecutableEmailAction` constructors with fewer parameters (no `HtmlSanitizer`), and `WatcherDateTimeUtils.parseTimeValue` called with two arguments. It also used `Settings.EMPTY` instead of `ImmutableSettings.EMPTY`.

**Parent 2** added an `htmlSanitizer` parameter to `EmailActionFactory` and `ExecutableEmailAction` constructors (**Change_Parameter_Type** / addition), and added a third argument to `parseTimeValue` calls (`Field.CONNECTION_TIMEOUT.toString()` / `Field.READ_TIMEOUT.toString()`). It also used `ImmutableSettings.EMPTY`.

## Interpretation

Two distinct refactorings are evidenced:

1. **Change_Parameter_Type / Parameterize_Variable** in `WatcherDateTimeUtils.parseTimeValue`: one parent added a third parameter (field name string) to this method. The other parent's callers only passed two arguments. The merge had to add the third argument in all call sites — the `--` lines show the two-argument call, and the `++` lines show the resolved three-argument call.

2. **Change_Parameter_Type** in `EmailActionFactory` and `ExecutableEmailAction` constructors: one parent added an `HtmlSanitizer` parameter. The other parent's callers still used the old two/three-arg constructor signatures. The merge added the `htmlSanitizer` argument at each call site, while simultaneously resolving the `ImmutableSettings` vs. `Settings` rename conflict. The `--` lines reflect one parent's call without `htmlSanitizer`, and the `++` lines reflect the merged call with `htmlSanitizer` added.

## Complete diff

```diff
diff --cc src/main/java/org/elasticsearch/watcher/support/http/HttpRequest.java
@@@ -204,18 +204,18 @@@
                     } else if (Field.CONNECTION_TIMEOUT.match(currentFieldName)) {
                         try {
 -                            builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                            builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.CONNECTION_TIMEOUT.toString()));
                         } catch (WatcherDateTimeUtils.ParseException pe) { ... }
                     } else if (Field.READ_TIMEOUT.match(currentFieldName)) {
                         try {
 -                            builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                            builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.READ_TIMEOUT.toString()));

diff --cc src/main/java/org/elasticsearch/watcher/support/http/HttpRequestTemplate.java
@@@ -244,28 +244,28 @@@
                 } else if (Field.CONNECTION_TIMEOUT.match(currentFieldName)) {
                     try {
 -                        builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                        builder.connectionTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.CONNECTION_TIMEOUT.toString()));
                     ...
                 } else if (Field.READ_TIMEOUT.match(currentFieldName)) {
                     try {
 -                        builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null));
++                        builder.readTimeout(WatcherDateTimeUtils.parseTimeValue(parser, null, Field.READ_TIMEOUT.toString()));

diff --cc src/test/java/org/elasticsearch/watcher/actions/email/EmailActionTests.java
@@@ -243,7 +243,7 @@@
-         ExecutableEmailAction executable = new EmailActionFactory(Settings.EMPTY, emailService, engine)
 -        ExecutableEmailAction executable = new EmailActionFactory(ImmutableSettings.EMPTY, emailService, engine, htmlSanitizer)
++        ExecutableEmailAction executable = new EmailActionFactory(Settings.EMPTY, emailService, engine, htmlSanitizer)

@@@ -335,7 +335,7 @@@
-         ExecutableEmailAction parsed = new EmailActionFactory(Settings.EMPTY, service, engine)
 -        ExecutableEmailAction parsed = new EmailActionFactory(ImmutableSettings.EMPTY, service, engine, htmlSanitizer)
++        ExecutableEmailAction parsed = new EmailActionFactory(Settings.EMPTY, service, engine, htmlSanitizer)

@@@ -364,7 +364,7 @@@
-         new EmailActionFactory(Settings.EMPTY, emailService, engine)
 -        new EmailActionFactory(ImmutableSettings.EMPTY, emailService, engine, htmlSanitizer)
++        new EmailActionFactory(Settings.EMPTY, emailService, engine, htmlSanitizer)

diff --cc src/test/java/org/elasticsearch/watcher/test/WatcherTestUtils.java
@@@ -195,7 +195,7 @@@
-         ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine);
 -        ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine, new HtmlSanitizer(ImmutableSettings.EMPTY));
++        ExecutableEmailAction executale = new ExecutableEmailAction(action, logger, emailService, templateEngine, new HtmlSanitizer(Settings.EMPTY));
```
