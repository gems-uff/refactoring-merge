# Project: cas — Merge commit SHA1: 0a46947b3778f7787f6cd4a3330f4dcc2729e76a

## Modified file(s):
- `cas-server-support-authy/src/main/java/org/apereo/cas/adaptors/authy/AuthyAccountRegistry.java`
- `cas-server-support-authy/src/main/java/org/apereo/cas/adaptors/authy/InMemoryAuthyAccountRegistry.java`
- `cas-server-support-authy/src/main/java/org/apereo/cas/adaptors/authy/AuthyAuthenticationHandler.java`

## Class(es) modified in the merge:
`AuthyAccountRegistry`, `InMemoryAuthyAccountRegistry`, `AuthyAuthenticationHandler`

## Merge effort lines in the combined diff

```diff
diff --cc cas-server-support-authy/src/main/java/org/apereo/cas/adaptors/authy/AuthyAccountRegistry.java

- package org.jasig.cas.adaptors;
++package org.apereo.cas.adaptors.authy;

- import org.jasig.cas.authentication.principal.Principal;
++import org.apereo.cas.authentication.principal.Principal;

-     void add(Long authyId, Principal principal);
++    void add(Integer authyId, Principal principal);

-     Long get(Principal principal);
- 
++    Integer get(Principal principal);
```

```diff
diff --cc cas-server-support-authy/src/main/java/org/apereo/cas/adaptors/authy/InMemoryAuthyAccountRegistry.java
(new file — all ++ lines)

++public class InMemoryAuthyAccountRegistry implements AuthyAccountRegistry {
++    private Map<Principal, Integer> store = new HashMap<>();
++
++    @Override
++    public void add(final Integer authyId, final Principal principal) {
++        store.put(principal, authyId);
++    }
++
++    @Override
++    public Integer get(final Principal principal) {
++        return store.get(principal);
++    }
++}
```

```diff
diff --cc cas-server-support-authy/src/main/java/org/apereo/cas/adaptors/authy/AuthyAuthenticationHandler.java

-         final long authyId = user.getId();
++            final Integer authyId = user.getId();
              this.registry.add(authyId, principal);

-         final Long authyId = this.registry.get(principal);
++        final Integer authyId = this.registry.get(principal);

-         final Token verification = this.authyTokens.verify(authyId.intValue(), ...);
++        final Token verification = this.authyTokens.verify(authyId, tokenCredential.getToken(), options);
```

## Relevant final code in the merge

```java
// AuthyAccountRegistry.java
public interface AuthyAccountRegistry {
    void add(Integer authyId, Principal principal);
    Integer get(Principal principal);
    boolean contains(Principal principal);
}

// InMemoryAuthyAccountRegistry.java
public class InMemoryAuthyAccountRegistry implements AuthyAccountRegistry {
    private Map<Principal, Integer> store = new HashMap<>();

    @Override
    public void add(final Integer authyId, final Principal principal) {
        store.put(principal, authyId);
    }

    @Override
    public Integer get(final Principal principal) {
        return store.get(principal);
    }
}

// AuthyAuthenticationHandler.java (excerpt)
final Integer authyId = user.getId();
this.registry.add(authyId, principal);
...
final Integer authyId = this.registry.get(principal);
final Token verification = this.authyTokens.verify(authyId, tokenCredential.getToken(), options);
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
8 lines (6 `++` replacements across interface and handler; 2 `--` removals of the old `Long` signatures)

## What each side had

- **Parent 1 (old branch):** `AuthyAccountRegistry` declared in package `org.jasig.cas.adaptors` using `Long` as the Authy ID type — `void add(Long authyId, Principal principal)` and `Long get(Principal principal)`. The handler stored and retrieved the ID as `long`/`Long` and called `authyTokens.verify(authyId.intValue(), ...)`.
- **Parent 2 (new CAS branch):** The module was restructured into `org.apereo.cas.adaptors.authy`. The Authy ID type was corrected from `Long` to `Integer` to match the actual Authy Java SDK return type. The interface signatures, implementations, and all usages in the handler needed to be updated accordingly.

## Interpretation

This case evidences **Change_Return_Type** and **Change_Parameter_Type** applied to the `AuthyAccountRegistry` interface. One branch restructured the Authy support module and aligned the Authy ID type with the real SDK (`Integer` instead of `Long`). The merge had to resolve the conflict by committing to the new `Integer` type throughout: the interface method signatures `add(Integer, Principal)` and `Integer get(Principal)`, the local variables in `AuthyAuthenticationHandler` (both where the ID is stored after user creation and where it is retrieved), and the `verify()` call which no longer requires `.intValue()`. The `++` and `--` lines across all three files directly reflect this type-change conflict resolution and are well-supported by the combined diff evidence.

