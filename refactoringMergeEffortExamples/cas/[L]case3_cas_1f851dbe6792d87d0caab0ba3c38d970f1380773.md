# Case 3 — Project: cas — Merge commit SHA1: 1f851dbe6792d87d0caab0ba3c38d970f1380773

## Modified file(s):
- `core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java`

## Class(es) modified in the merge:
`CasCoreAuditConfiguration`

## Merge effort lines in the combined diff

```diff
diff --cc core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java
index 0addb399961,5b01554ca5f..1d8da24e0ed
--- a/core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java
+++ b/core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java
@@@ -6,18 -6,18 +6,18 @@@

- import org.apereo.cas.audit.spi.ChainingAuditPrincipalIdProvider;
- import org.apereo.cas.audit.spi.CredentialsAsFirstParameterResourceResolver;
- import org.apereo.cas.audit.spi.DefaultAuditTrailExecutionPlan;
- import org.apereo.cas.audit.spi.DefaultAuditTrailRecordResolutionPlan;
- import org.apereo.cas.audit.spi.MessageBundleAwareResourceResolver;
- import org.apereo.cas.audit.spi.NullableReturnValueAuditResourceResolver;
- import org.apereo.cas.audit.spi.ServiceAccessEnforcementAuditResourceResolver;
- import org.apereo.cas.audit.spi.ServiceResourceResolver;
- import org.apereo.cas.audit.spi.ShortenedReturnValueAsStringResourceResolver;
- import org.apereo.cas.audit.spi.ThreadLocalPrincipalResolver;
- import org.apereo.cas.audit.spi.TicketAsFirstParameterResourceResolver;
- import org.apereo.cas.audit.spi.TicketValidationResourceResolver;
 -import org.apereo.cas.audit.spi.principal.ChainingAuditPrincipalIdProvider;
 -import org.apereo.cas.audit.spi.resource.CredentialsAsFirstParameterResourceResolver;
+  import org.apereo.cas.audit.spi.plan.DefaultAuditTrailExecutionPlan;
+  import org.apereo.cas.audit.spi.plan.DefaultAuditTrailRecordResolutionPlan;
++import org.apereo.cas.audit.spi.principal.ChainingAuditPrincipalIdProvider;
++import org.apereo.cas.audit.spi.principal.ThreadLocalPrincipalResolver;
++import org.apereo.cas.audit.spi.resource.CredentialsAsFirstParameterResourceResolver;
+  import org.apereo.cas.audit.spi.resource.MessageBundleAwareResourceResolver;
+  import org.apereo.cas.audit.spi.resource.NullableReturnValueAuditResourceResolver;
+  import org.apereo.cas.audit.spi.resource.ServiceAccessEnforcementAuditResourceResolver;
+  import org.apereo.cas.audit.spi.resource.ServiceResourceResolver;
+  import org.apereo.cas.audit.spi.resource.ShortenedReturnValueAsStringResourceResolver;
 -import org.apereo.cas.audit.spi.principal.ThreadLocalPrincipalResolver;
+  import org.apereo.cas.audit.spi.resource.TicketAsFirstParameterResourceResolver;
+  import org.apereo.cas.audit.spi.resource.TicketValidationResourceResolver;
```

## Relevant final code in the merge

```java
import org.apereo.cas.audit.spi.plan.DefaultAuditTrailExecutionPlan;
import org.apereo.cas.audit.spi.plan.DefaultAuditTrailRecordResolutionPlan;
import org.apereo.cas.audit.spi.principal.ChainingAuditPrincipalIdProvider;
import org.apereo.cas.audit.spi.principal.ThreadLocalPrincipalResolver;
import org.apereo.cas.audit.spi.resource.CredentialsAsFirstParameterResourceResolver;
import org.apereo.cas.audit.spi.resource.MessageBundleAwareResourceResolver;
import org.apereo.cas.audit.spi.resource.NullableReturnValueAuditResourceResolver;
import org.apereo.cas.audit.spi.resource.ServiceAccessEnforcementAuditResourceResolver;
import org.apereo.cas.audit.spi.resource.ServiceResourceResolver;
import org.apereo.cas.audit.spi.resource.ShortenedReturnValueAsStringResourceResolver;
import org.apereo.cas.audit.spi.resource.TicketAsFirstParameterResourceResolver;
import org.apereo.cas.audit.spi.resource.TicketValidationResourceResolver;
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
3 lines (`++import org.apereo.cas.audit.spi.principal.ChainingAuditPrincipalIdProvider`, `++import org.apereo.cas.audit.spi.principal.ThreadLocalPrincipalResolver`, `++import org.apereo.cas.audit.spi.resource.CredentialsAsFirstParameterResourceResolver`)

## What each side had

- **Parent 1 (old):** All audit SPI classes (`ChainingAuditPrincipalIdProvider`, `ThreadLocalPrincipalResolver`, `CredentialsAsFirstParameterResourceResolver`, and all resource resolvers) resided in the flat package `org.apereo.cas.audit.spi`.
- **Parent 2 (new):** One branch moved classes into sub-packages — principal-related classes to `org.apereo.cas.audit.spi.principal`, resource-related classes to `org.apereo.cas.audit.spi.resource`, and plan-related classes to `org.apereo.cas.audit.spi.plan`. The two parents split on exactly which classes had been moved: one parent had `ChainingAuditPrincipalIdProvider` and `CredentialsAsFirstParameterResourceResolver` in `spi.principal` / `spi.resource`, while the other had `ThreadLocalPrincipalResolver` in `spi.principal`. Neither parent alone provided the full correct set of three `++` imports.

## Interpretation

This case evidences **Move_Class** applied surgically to multiple classes in the `org.apereo.cas.audit.spi` package. One parent moved classes into `spi.plan`, `spi.principal`, and `spi.resource`. A second parent independently moved a partially overlapping set of those same classes. The three `++` import lines — `ChainingAuditPrincipalIdProvider`, `ThreadLocalPrincipalResolver`, and `CredentialsAsFirstParameterResourceResolver` — represent the merger's work to reconcile both movement paths. The conflict is confirmed as surgical and not a mass namespace migration because only these three imports required `++` treatment while the remaining nine imports could be resolved directly from one of the parents. The combined diff notation (` -` for P2 discard, `- ` for P1 discard, `++` for merge resolution) makes the three-way conflict unambiguous.

## Complete diff

```diff
diff --cc core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java
index 0addb399961,5b01554ca5f..1d8da24e0ed
--- a/core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java
+++ b/core/cas-server-core-audit/src/main/java/org/apereo/cas/audit/spi/config/CasCoreAuditConfiguration.java
@@@ -6,18 -6,18 +6,18 @@@ import org.apereo.cas.audit.AuditTrailE
  import org.apereo.cas.audit.AuditTrailExecutionPlanConfigurer;
  import org.apereo.cas.audit.AuditTrailRecordResolutionPlan;
  import org.apereo.cas.audit.AuditTrailRecordResolutionPlanConfigurer;
- import org.apereo.cas.audit.spi.ChainingAuditPrincipalIdProvider;
- import org.apereo.cas.audit.spi.CredentialsAsFirstParameterResourceResolver;
- import org.apereo.cas.audit.spi.DefaultAuditTrailExecutionPlan;
- import org.apereo.cas.audit.spi.DefaultAuditTrailRecordResolutionPlan;
- import org.apereo.cas.audit.spi.MessageBundleAwareResourceResolver;
- import org.apereo.cas.audit.spi.NullableReturnValueAuditResourceResolver;
- import org.apereo.cas.audit.spi.ServiceAccessEnforcementAuditResourceResolver;
- import org.apereo.cas.audit.spi.ServiceResourceResolver;
- import org.apereo.cas.audit.spi.ShortenedReturnValueAsStringResourceResolver;
- import org.apereo.cas.audit.spi.ThreadLocalPrincipalResolver;
- import org.apereo.cas.audit.spi.TicketAsFirstParameterResourceResolver;
- import org.apereo.cas.audit.spi.TicketValidationResourceResolver;
 -import org.apereo.cas.audit.spi.principal.ChainingAuditPrincipalIdProvider;
 -import org.apereo.cas.audit.spi.resource.CredentialsAsFirstParameterResourceResolver;
+  import org.apereo.cas.audit.spi.plan.DefaultAuditTrailExecutionPlan;
+  import org.apereo.cas.audit.spi.plan.DefaultAuditTrailRecordResolutionPlan;
++import org.apereo.cas.audit.spi.principal.ChainingAuditPrincipalIdProvider;
++import org.apereo.cas.audit.spi.principal.ThreadLocalPrincipalResolver;
++import org.apereo.cas.audit.spi.resource.CredentialsAsFirstParameterResourceResolver;
+  import org.apereo.cas.audit.spi.resource.MessageBundleAwareResourceResolver;
+  import org.apereo.cas.audit.spi.resource.NullableReturnValueAuditResourceResolver;
+  import org.apereo.cas.audit.spi.resource.ServiceAccessEnforcementAuditResourceResolver;
+  import org.apereo.cas.audit.spi.resource.ServiceResourceResolver;
+  import org.apereo.cas.audit.spi.resource.ShortenedReturnValueAsStringResourceResolver;
 -import org.apereo.cas.audit.spi.principal.ThreadLocalPrincipalResolver;
+  import org.apereo.cas.audit.spi.resource.TicketAsFirstParameterResourceResolver;
+  import org.apereo.cas.audit.spi.resource.TicketValidationResourceResolver;
  import org.apereo.cas.configuration.CasConfigurationProperties;
  import org.apereo.cas.util.CollectionUtils;
```
