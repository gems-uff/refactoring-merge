# Case 4 — Project: cas — Merge commit SHA1: 5c7925b7fc861fdccc2548d03af82415d03c251a

## Modified file(s):
- `support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java`

## Class(es) modified in the merge:
`OAuth20HandlerInterceptorAdapter`

## Merge effort lines in the combined diff

```diff
diff --cc support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java
index 7a18c7fc50d,cdb7e319bbb..48999307478
--- a/support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java
+++ b/support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java
@@@ -39,7 -36,7 +38,7 @@@
      @Override
      public boolean preHandle(final HttpServletRequest request, final HttpServletResponse response,
                               final Object handler) throws Exception {
--        if (isAccessTokenRequestRequest(request, response)) {
++        if (requestRequiresAuthentication(request, response)) {
              return requiresAuthenticationAccessTokenInterceptor.preHandle(request, response, handler);
          }

@@@ -56,22 -53,21 +55,42 @@@
      /**
       * ...
       */
--    protected boolean isAccessTokenRequestRequest(final HttpServletRequest request, final HttpServletResponse response) {
++    protected boolean isAccessTokenRequest(final HttpServletRequest request, final HttpServletResponse response) {
          val requestPath = request.getRequestURI();
--        val value = doesUriMatchPattern(requestPath, OAuth20Constants.ACCESS_TOKEN_URL)
-                 || doesUriMatchPattern(requestPath, OAuth20Constants.TOKEN_URL);
 -            || doesUriMatchPattern(requestPath, OAuth20Constants.TOKEN_URL);
--        if (!value) {
--            val extractor = this.accessTokenGrantRequestExtractors
-                     .stream()
-                     .filter(ext -> ext.supports(request))
-                     .findFirst()
-                     .orElse(null);
 -                .stream()
 -                .filter(ext -> ext.supports(request))
 -                .findFirst()
 -                .orElse(null);
--            if (extractor != null) {
-                 return extractor.getGrantType() == OAuth20GrantTypes.CLIENT_CREDENTIALS
-                         || extractor.getGrantType() == OAuth20GrantTypes.PASSWORD;
 -                return extractor.requestMustBeAuthenticated();
++        val pattern = String.format("(%s|%s)", OAuth20Constants.ACCESS_TOKEN_URL, OAuth20Constants.TOKEN_URL);
++        return doesUriMatchPattern(requestPath, pattern);
++    }
++
++    protected boolean requestRequiresAuthentication(final HttpServletRequest request, final HttpServletResponse response) {
++        val accessTokenRequest = isAccessTokenRequest(request, response);
++        if (!accessTokenRequest) {
++            val extractor = extractAccessTokenGrantRequest(request);
++            if (extractor.isPresent()) {
++                val ext = extractor.get();
++                return ext.requestMustBeAuthenticated();
++            }
++        } else {
++            val extractor = extractAccessTokenGrantRequest(request);
++            if (extractor.isPresent()) {
++                val ext = extractor.get();
++                return ext.getResponseType() != OAuth20ResponseTypes.DEVICE_CODE;
++            }
++        }
--        return value;
++        return false;
++    }
++
++    private Optional<AccessTokenGrantRequestExtractor> extractAccessTokenGrantRequest(final HttpServletRequest request) {
++        return this.accessTokenGrantRequestExtractors
++            .stream()
++            .filter(ext -> ext.supports(request))
++            .findFirst();
++    }
```

## Relevant final code in the merge

```java
@Override
public boolean preHandle(final HttpServletRequest request, final HttpServletResponse response,
                         final Object handler) throws Exception {
    if (requestRequiresAuthentication(request, response)) {
        return requiresAuthenticationAccessTokenInterceptor.preHandle(request, response, handler);
    }
    ...
}

protected boolean isAccessTokenRequest(final HttpServletRequest request, final HttpServletResponse response) {
    val requestPath = request.getRequestURI();
    val pattern = String.format("(%s|%s)", OAuth20Constants.ACCESS_TOKEN_URL, OAuth20Constants.TOKEN_URL);
    return doesUriMatchPattern(requestPath, pattern);
}

protected boolean requestRequiresAuthentication(final HttpServletRequest request, final HttpServletResponse response) {
    val accessTokenRequest = isAccessTokenRequest(request, response);
    if (!accessTokenRequest) {
        val extractor = extractAccessTokenGrantRequest(request);
        if (extractor.isPresent()) {
            return extractor.get().requestMustBeAuthenticated();
        }
    } else {
        val extractor = extractAccessTokenGrantRequest(request);
        if (extractor.isPresent()) {
            return extractor.get().getResponseType() != OAuth20ResponseTypes.DEVICE_CODE;
        }
    }
    return false;
}

private Optional<AccessTokenGrantRequestExtractor> extractAccessTokenGrantRequest(final HttpServletRequest request) {
    return this.accessTokenGrantRequestExtractors
        .stream()
        .filter(ext -> ext.supports(request))
        .findFirst();
}
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
23 lines (2 `--` for the old method name at the declaration and call site; 21 `++` for the renamed method body, the new extracted method `requestRequiresAuthentication`, the private helper `extractAccessTokenGrantRequest`, and the updated call in `preHandle`)

## What each side had

- **Parent 1:** A single method `isAccessTokenRequestRequest(request, response)` (note the doubled word "Request") that combined URL matching and extractor-based grant type checking (`OAuth20GrantTypes.CLIENT_CREDENTIALS` or `PASSWORD`) in one block. This method was called directly from `preHandle`.
- **Parent 2:** The same method was split: its name was corrected to `isAccessTokenRequest` with URL-only responsibility, and a new gate method `requestRequiresAuthentication` was extracted into `preHandle`'s position. The internal logic was also extended to handle `OAuth20ResponseTypes.DEVICE_CODE` (a new grant type added on this branch).

## Interpretation

This case primarily evidences **Rename_Method**: `isAccessTokenRequestRequest` was renamed to `isAccessTokenRequest`, correcting the duplicated word and narrowing the method's responsibility to URL pattern matching only. Additionally, **Extract_Method** is evidenced: the broader authentication-gate logic was extracted into the new `requestRequiresAuthentication` method, which became the single entry point in `preHandle`. The `--` lines mark the removal of the old doubled-name method signature and its call site, while the `++` lines introduce the renamed method with its simplified body, the new extracted method with device-code-aware logic, and the private helper `extractAccessTokenGrantRequest`. The conflict arose because both parents had modified the same method region — one renaming and splitting it, the other modifying its internal grant-type logic — making the merge effort lines directly attributable to the refactoring performed in one of the parents.

## Complete diff

```diff
diff --cc support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java
index 7a18c7fc50d,cdb7e319bbb..48999307478
--- a/support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java
+++ b/support/cas-server-support-oauth/src/main/java/org/apereo/cas/support/oauth/web/OAuth20HandlerInterceptorAdapter.java
@@@ -1,17 -1,15 +1,17 @@@
  package org.apereo.cas.support.oauth.web;

- import lombok.val;
- import lombok.AllArgsConstructor;
+ import lombok.RequiredArgsConstructor;
  import lombok.extern.slf4j.Slf4j;
+ import lombok.val;
  import org.apereo.cas.support.oauth.OAuth20Constants;
- import org.apereo.cas.support.oauth.OAuth20GrantTypes;
- import org.apereo.cas.support.oauth.web.response.accesstoken.ext.BaseAccessTokenGrantRequestExtractor;
++import org.apereo.cas.support.oauth.OAuth20ResponseTypes;
+ import org.apereo.cas.support.oauth.web.response.accesstoken.ext.AccessTokenGrantRequestExtractor;
  import org.springframework.web.servlet.handler.HandlerInterceptorAdapter;

  import javax.servlet.http.HttpServletRequest;
  import javax.servlet.http.HttpServletResponse;
  import java.util.Collection;
++import java.util.Optional;
  import java.util.regex.Pattern;

@@@ -39,7 -36,7 +38,7 @@@
      @Override
      public boolean preHandle(final HttpServletRequest request, final HttpServletResponse response,
                               final Object handler) throws Exception {
--        if (isAccessTokenRequestRequest(request, response)) {
++        if (requestRequiresAuthentication(request, response)) {
              return requiresAuthenticationAccessTokenInterceptor.preHandle(request, response, handler);
          }

@@@ -56,22 -53,21 +55,42 @@@
--    protected boolean isAccessTokenRequestRequest(final HttpServletRequest request, final HttpServletResponse response) {
++    protected boolean isAccessTokenRequest(final HttpServletRequest request, final HttpServletResponse response) {
          val requestPath = request.getRequestURI();
--        val value = doesUriMatchPattern(requestPath, OAuth20Constants.ACCESS_TOKEN_URL)
-                 || doesUriMatchPattern(requestPath, OAuth20Constants.TOKEN_URL);
 -            || doesUriMatchPattern(requestPath, OAuth20Constants.TOKEN_URL);
--        if (!value) {
--            val extractor = this.accessTokenGrantRequestExtractors
-                     .stream()
-                     .filter(ext -> ext.supports(request))
-                     .findFirst()
-                     .orElse(null);
 -                .stream()
 -                .filter(ext -> ext.supports(request))
 -                .findFirst()
 -                .orElse(null);
--            if (extractor != null) {
-                 return extractor.getGrantType() == OAuth20GrantTypes.CLIENT_CREDENTIALS
-                         || extractor.getGrantType() == OAuth20GrantTypes.PASSWORD;
 -                return extractor.requestMustBeAuthenticated();
++        val pattern = String.format("(%s|%s)", OAuth20Constants.ACCESS_TOKEN_URL, OAuth20Constants.TOKEN_URL);
++        return doesUriMatchPattern(requestPath, pattern);
++    }
++
++    protected boolean requestRequiresAuthentication(final HttpServletRequest request, final HttpServletResponse response) {
++        val accessTokenRequest = isAccessTokenRequest(request, response);
++        if (!accessTokenRequest) {
++            val extractor = extractAccessTokenGrantRequest(request);
++            if (extractor.isPresent()) {
++                val ext = extractor.get();
++                return ext.requestMustBeAuthenticated();
++            }
++        } else {
++            val extractor = extractAccessTokenGrantRequest(request);
++            if (extractor.isPresent()) {
++                val ext = extractor.get();
++                return ext.getResponseType() != OAuth20ResponseTypes.DEVICE_CODE;
++            }
          }
--        return value;
++        return false;
++    }
++
++    private Optional<AccessTokenGrantRequestExtractor> extractAccessTokenGrantRequest(final HttpServletRequest request) {
++        return this.accessTokenGrantRequestExtractors
++            .stream()
++            .filter(ext -> ext.supports(request))
++            .findFirst();
++    }
```
