# Project: RxJava — Merge commit SHA1: 1c47b0cb26e6d971eefa42ebb3479099236b0125

## Modified file(s):
- `rxjava-core/src/main/java/rx/operators/OperationThrottle.java`

## Class(es) modified in the merge:
- `OperationThrottle`
- `Throttle<T>`
- `ThrottledObserver<T>`
- `ThrottledOnNext<T>`
- `ThrottledOnError<T>`
- `ThrottledOnComplete<T>`

## Merge effort lines in the combined diff

```diff
 +import rx.Observable.OnSubscribeFunc;

 -    public static <T> Func1<Observer<T>, Subscription> throttle(Observable<T> items, long timeout, TimeUnit unit) {
++    public static <T> OnSubscribeFunc<T> throttle(Observable<T> items, long timeout, TimeUnit unit) {

 -    public static <T> Func1<Observer<T>, Subscription> throttle(final Observable<T> items, final long timeout, final TimeUnit unit, final Scheduler scheduler) {
 -        return new Func1<Observer<T>, Subscription>() {
++    public static <T> OnSubscribeFunc<T> throttle(final Observable<T> items, final long timeout, final TimeUnit unit, final Scheduler scheduler) {
++        return new OnSubscribeFunc<T>() {
++            public Subscription onSubscribe(Observer<? super T> observer) {
++                return new Throttle<T>(items, timeout, unit, scheduler).onSubscribe(observer);

 -    private static class Throttle<T> implements Func1<Observer<T>, Subscription> {
++    private static class Throttle<T> implements OnSubscribeFunc<T> {

 -        public Subscription call(Observer<T> observer) {
++        public Subscription onSubscribe(Observer<? super T> observer) {

 -        private final Observer<T> observer;
++        private final Observer<? super T> observer;

 -        public ThrottledObserver(Observer<T> observer, long timeout, TimeUnit unit, Scheduler scheduler) {
++        public ThrottledObserver(Observer<? super T> observer, long timeout, TimeUnit unit, Scheduler scheduler) {

 -        public void onError(Exception e) {
++        public void onError(Throwable e) {

 -        private final Observer<T> observer;
++        private final Observer<? super T> observer;

 -        public ThrottledOnNext(Observer<T> observer, T value) {
++        public ThrottledOnNext(Observer<? super T> observer, T value) {

 -        private final Observer<T> observer;
 -        private final Exception error;
++        private final Observer<? super T> observer;
++        private final Throwable error;

 -        public ThrottledOnError(Observer<T> observer, Exception error) {
++        public ThrottledOnError(Observer<? super T> observer, Throwable error) {

 -        private final Observer<T> observer;
++        private final Observer<? super T> observer;

 -        public ThrottledOnComplete(Observer<T> observer) {
++        public ThrottledOnComplete(Observer<? super T> observer) {

 -            Observable<String> source = Observable.create(new Func1<Observer<String>, Subscription>() {
++            Observable<String> source = Observable.create(new OnSubscribeFunc<String>() {
++                public Subscription onSubscribe(Observer<? super String> observer) {

 -            Observable<String> source = Observable.create(new Func1<Observer<String>, Subscription>() {
++            Observable<String> source = Observable.create(new OnSubscribeFunc<String>() {
++                public Subscription onSubscribe(Observer<? super String> observer) {
```

## Relevant final code in the merge

```java
public static <T> OnSubscribeFunc<T> throttle(Observable<T> items, long timeout, TimeUnit unit) {
    return throttle(items, timeout, unit, Schedulers.executor(...));
}

public static <T> OnSubscribeFunc<T> throttle(final Observable<T> items, final long timeout,
        final TimeUnit unit, final Scheduler scheduler) {
    return new OnSubscribeFunc<T>() {
        @Override
        public Subscription onSubscribe(Observer<? super T> observer) {
            return new Throttle<T>(items, timeout, unit, scheduler).onSubscribe(observer);
        }
    };
}

private static class Throttle<T> implements OnSubscribeFunc<T> {
    @Override
    public Subscription onSubscribe(Observer<? super T> observer) {
        return items.subscribe(new ThrottledObserver<T>(observer, timeout, unit, scheduler));
    }
}

private static class ThrottledObserver<T> implements Observer<T> {
    private final Observer<? super T> observer;
    @Override
    public void onError(Throwable e) { ... }
}
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
28 lines

## What each side had

**Parent 1** had `OperationThrottle` using the old functional API:
- Return type: `Func1<Observer<T>, Subscription>`
- Method name: `call(Observer<T> observer)`
- Observer bound: `Observer<T>` (invariant)
- Error type: `Exception`
- Anonymous class: `new Func1<Observer<T>, Subscription>()`

**Parent 2** introduced the new API type `OnSubscribeFunc<T>` in the main RxJava branch, renaming the subscription factory interface and widening the observer bound to `Observer<? super T>`, and replacing `Exception` with `Throwable` in `onError`.

## Interpretation

This case evidences three closely related refactoring types applied in **Parent 2** before the merge:

1. **Rename_Method** (`call` → `onSubscribe`): The subscription callback method was renamed across the entire `OperationThrottle` class and its inner classes (`Throttle`, anonymous lambdas, test helpers). The merge had to reconcile the new API name with the existing implementation inherited from Parent 1.

2. **Change_Return_Type** (`Func1<Observer<T>, Subscription>` → `OnSubscribeFunc<T>`): The factory method return type changed from the generic `Func1` to the new dedicated interface `OnSubscribeFunc`. Every occurrence of the old type in method signatures and `implements` clauses required a `++` rewrite.

3. **Rename_Attribute** / **Change_Parameter_Type** (`Observer<T>` → `Observer<? super T>`): The observer fields in `ThrottledObserver`, `ThrottledOnNext`, `ThrottledOnError`, and `ThrottledOnComplete` were widened to use the covariant wildcard. Combined with `Exception` → `Throwable` in `onError`, the merge had to adapt every field declaration and constructor parameter.

The `++` lines confirm all three: the new import `rx.Observable.OnSubscribeFunc`, every method signature rewrite, and every field type change were produced during the merge to reconcile Parent 1's implementation with Parent 2's API refactoring. The ` -` parent-1 context lines show exactly what was replaced.

## Complete diff

```diff
diff --cc rxjava-core/src/main/java/rx/Observable.java
index 2ac0f42754,35512dcf7a..b0edd4e370
--- a/rxjava-core/src/main/java/rx/Observable.java
+++ b/rxjava-core/src/main/java/rx/Observable.java
@@@ -64,7 -65,10 +64,8 @@@ import rx.operators.OperationTake
  import rx.operators.OperationTakeLast;
  import rx.operators.OperationTakeUntil;
  import rx.operators.OperationTakeWhile;
+ import rx.operators.OperationThrottle;
  import rx.operators.OperationTimestamp;
 -import rx.operators.OperationToFuture;
 -import rx.operators.OperationToIterator;
  import rx.operators.OperationToObservableFuture;
  import rx.operators.OperationToObservableIterable;
  import rx.operators.OperationToObservableList;
@@@ -2242,43 -2070,73 +2243,66 @@@ public class Observable<T>
  [... large section removed for brevity - see raw diff ...]

diff --cc rxjava-core/src/main/java/rx/operators/OperationThrottle.java
index 0000000000,ebef4e7286..0bee16043d
mode 000000,100644..100644
--- a/rxjava-core/src/main/java/rx/operators/OperationThrottle.java
+++ b/rxjava-core/src/main/java/rx/operators/OperationThrottle.java
@@@ -1,0 -1,311 +1,312 @@@
+ [full file content with ++ merge effort lines as shown above]
```
