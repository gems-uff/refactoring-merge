# Case 2 — Project: hive — Merge commit SHA1: 5592c0f1cd11cb31744928207e64865558cf70c4

## Modified file(s):
- `llap-server/src/java/org/apache/hadoop/hive/llap/io/metadata/OrcStripeMetadata.java`
- `llap-server/src/java/org/apache/hadoop/hive/llap/io/encoded/OrcEncodedDataProducer.java`

## Class(es) modified in the merge:
- `OrcStripeMetadata`

## Merge effort lines in the combined diff

### OrcStripeMetadata.java — `Rename_Attribute` (`rowIndexes` → `rowIndex`) + `Change_Parameter_Type` in constructor and methods

```diff
+import org.apache.hadoop.hive.ql.io.orc.RecordReaderImpl;
 import org.apache.hadoop.hive.ql.io.orc.StripeInformation;

 public class OrcStripeMetadata {
   List<ColumnEncoding> encodings;
   List<Stream> streams;
-  RowIndex[] rowIndexes;
++  RecordReaderImpl.Index rowIndex;

-  public OrcStripeMetadata(
-      MetadataReader mr, StripeInformation stripe, boolean[] includes) throws IOException {
++  public OrcStripeMetadata(MetadataReader mr, StripeInformation stripe,
++      boolean[] includes, boolean[] sargColumns) throws IOException {
     StripeFooter footer = mr.readStripeFooter(stripe);
     streams = footer.getStreamsList();
     encodings = footer.getColumnsList();
-    rowIndexes = mr.readRowIndex(stripe, footer, includes, null);
++    rowIndex = mr.readRowIndex(stripe, footer, includes, null, sargColumns, null);
   }

   public boolean hasAllIndexes(boolean[] includes) {
     for (int i = 0; i < includes.length; ++i) {
-      if (includes[i] && rowIndexes[i] == null) return false;
++      if (includes[i] && rowIndex.getRowGroupIndex()[i] == null) return false;
     }
     return true;
   }

-  public void loadMissingIndexes(
-      MetadataReader mr, StripeInformation stripe, boolean[] includes) throws IOException {
++  public void loadMissingIndexes(MetadataReader mr, StripeInformation stripe, boolean[] includes,
++      boolean[] sargColumns) throws IOException {
-    rowIndexes = mr.readRowIndex(stripe, null, includes, rowIndexes);
++    rowIndex = mr.readRowIndex(stripe, null, includes, rowIndex.getRowGroupIndex(),
++        sargColumns, rowIndex.getBloomFilterIndex());
   }

   public RowIndex[] getRowIndexes() {
-    return rowIndexes;
++    return rowIndex.getRowGroupIndex();
   }

   public void setRowIndexes(RowIndex[] rowIndexes) {
-    this.rowIndexes = rowIndexes;
++    this.rowIndex.setRowGroupIndex(rowIndexes);
   }
```

### OrcEncodedDataProducer.java — call sites updated (sargColumns added)

```diff
++import org.apache.hadoop.hive.ql.io.orc.RecordReaderImpl;
...
++          sargColumns = new boolean[globalIncludes.length];
++          for (int i : filterColumns) {
++            if (i > 0) {
++              sargColumns[i] = true;
++            }
++          }
...
++          stripeMetadatas = readStripesMetadata(metadata, globalIncludes, sargColumns);
...
++              stripeMetadata = new OrcStripeMetadata(metadataReader, si, stripeIncludes, sargColumns);
++            updateLoadedIndexes(stripeMetadata, si, stripeIncludes, sargColumns);
...
++        StripeInformation stripe, boolean[] stripeIncludes, boolean[] sargColumns) throws IOException {
++        stripeMetadata.loadMissingIndexes(metadataReader, stripe, stripeIncludes, sargColumns);
...
++        OrcFileMetadata metadata, boolean[] globalInc, boolean[] sargColumns) throws IOException {
++            value = new OrcStripeMetadata(metadataReader, si, globalInc, sargColumns);
++            updateLoadedIndexes(value, si, globalInc, sargColumns);
...
++        sargApp = new SargApplier(sarg, colNamesForSarg, rowIndexStride, types);
```

## Relevant final code in the merge

```java
// OrcStripeMetadata.java
import org.apache.hadoop.hive.ql.io.orc.RecordReaderImpl;
...
public class OrcStripeMetadata {
    List<ColumnEncoding> encodings;
    List<Stream> streams;
    RecordReaderImpl.Index rowIndex;   // was: RowIndex[] rowIndexes

    public OrcStripeMetadata(MetadataReader mr, StripeInformation stripe,
            boolean[] includes, boolean[] sargColumns) throws IOException {  // was: (mr, stripe, includes)
        StripeFooter footer = mr.readStripeFooter(stripe);
        streams = footer.getStreamsList();
        encodings = footer.getColumnsList();
        rowIndex = mr.readRowIndex(stripe, footer, includes, null, sargColumns, null);
    }

    public boolean hasAllIndexes(boolean[] includes) {
        for (int i = 0; i < includes.length; ++i) {
            if (includes[i] && rowIndex.getRowGroupIndex()[i] == null) return false;
        }
        return true;
    }

    public void loadMissingIndexes(MetadataReader mr, StripeInformation stripe,
            boolean[] includes, boolean[] sargColumns) throws IOException {
        rowIndex = mr.readRowIndex(stripe, null, includes, rowIndex.getRowGroupIndex(),
                sargColumns, rowIndex.getBloomFilterIndex());
    }

    public RowIndex[] getRowIndexes() {
        return rowIndex.getRowGroupIndex();
    }

    public void setRowIndexes(RowIndex[] rowIndexes) {
        this.rowIndex.setRowGroupIndex(rowIndexes);
    }
}
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
**~30 lines** (++ lines: new field type `RecordReaderImpl.Index rowIndex`, new constructor signature, updated `hasAllIndexes()`, `loadMissingIndexes()` with added parameter, `getRowIndexes()`/`setRowIndexes()` delegating through new field; plus call-site updates in `OrcEncodedDataProducer`)

## What each side had

**Parent 1 (P1)** had `OrcStripeMetadata` with:
- A field `RowIndex[] rowIndexes` (plain array of row group indexes)
- Constructor: `OrcStripeMetadata(MetadataReader mr, StripeInformation stripe, boolean[] includes)` — no `sargColumns` parameter
- `loadMissingIndexes(MetadataReader, StripeInformation, boolean[] includes)` — no `sargColumns`
- `readRowIndex()` call: `mr.readRowIndex(stripe, footer, includes, null)` (4 args)

**Parent 2 (P2)** had a richer `RecordReaderImpl.Index` wrapper type (which holds both row group indexes AND bloom filter indexes) that P2 introduced to support bloom filter-based predicate pushdown. P2 renamed the field from `rowIndexes` (array) to `rowIndex` (wrapper object) and extended method signatures to pass `sargColumns` (needed for loading bloom filter data).

## Interpretation

Two refactorings are tightly coupled in this merge:

1. **Rename_Attribute** (`rowIndexes` → `rowIndex`): P2 introduced a richer wrapper type `RecordReaderImpl.Index` that encapsulates both `RowIndex[]` (row group indexes) and `BloomFilterIndex[]` (bloom filter indexes). The field name changed from the plural `rowIndexes` (array) to the singular `rowIndex` (composite object). The `--` lines confirm P1's `RowIndex[] rowIndexes`, while the `++` lines show `RecordReaderImpl.Index rowIndex`. Every reference to the field was updated: `hasAllIndexes()`, `loadMissingIndexes()`, `getRowIndexes()`, and `setRowIndexes()`.



## Complete diff

```diff
diff --cc llap-server/src/java/org/apache/hadoop/hive/llap/io/encoded/OrcEncodedDataProducer.java
index ...
+++ b/.../OrcEncodedDataProducer.java
@@@ ... @@@
++import org.apache.hadoop.hive.ql.io.orc.RecordReaderImpl;
...
++      boolean[] sargColumns = null;
++      if (sarg != null && stride != 0) {
++          int[] filterColumns = RecordReaderImpl.mapSargColumns(sarg.getLeaves(), columnNames, 0);
++          sargColumns = new boolean[globalIncludes.length];
++          for (int i : filterColumns) {
++            if (i > 0) { sargColumns[i] = true; }
++          }
++          stripeMetadatas = readStripesMetadata(metadata, globalIncludes, sargColumns);
++      }
...
++              stripeMetadata = new OrcStripeMetadata(metadataReader, si, stripeIncludes, sargColumns);
...
++              updateLoadedIndexes(stripeMetadata, si, stripeIncludes, sargColumns);
...
++  private void updateLoadedIndexes(OrcStripeMetadata stripeMetadata,
++      StripeInformation stripe, boolean[] stripeIncludes, boolean[] sargColumns) throws IOException {
++        stripeMetadata.loadMissingIndexes(metadataReader, stripe, stripeIncludes, sargColumns);
...
++  private ArrayList<OrcStripeMetadata> readStripesMetadata(
++      OrcFileMetadata metadata, boolean[] globalInc, boolean[] sargColumns) throws IOException {
++          value = new OrcStripeMetadata(metadataReader, si, globalInc, sargColumns);
++          updateLoadedIndexes(value, si, globalInc, sargColumns);

diff --cc llap-server/src/java/org/apache/hadoop/hive/llap/io/metadata/OrcStripeMetadata.java
index ...
+++ b/.../OrcStripeMetadata.java
@@@ -1,80 -1,0 +1,82 @@@
+import org.apache.hadoop.hive.ql.io.orc.RecordReaderImpl;
 ...
 public class OrcStripeMetadata {
   List<ColumnEncoding> encodings;
   List<Stream> streams;
-  RowIndex[] rowIndexes;
++  RecordReaderImpl.Index rowIndex;

-  public OrcStripeMetadata(
-      MetadataReader mr, StripeInformation stripe, boolean[] includes) throws IOException {
++  public OrcStripeMetadata(MetadataReader mr, StripeInformation stripe,
++      boolean[] includes, boolean[] sargColumns) throws IOException {
     ...
-    rowIndexes = mr.readRowIndex(stripe, footer, includes, null);
++    rowIndex = mr.readRowIndex(stripe, footer, includes, null, sargColumns, null);
   }

   public boolean hasAllIndexes(boolean[] includes) {
     for (int i = 0; i < includes.length; ++i) {
-      if (includes[i] && rowIndexes[i] == null) return false;
++      if (includes[i] && rowIndex.getRowGroupIndex()[i] == null) return false;
     }
     return true;
   }

-  public void loadMissingIndexes(
-      MetadataReader mr, StripeInformation stripe, boolean[] includes) throws IOException {
++  public void loadMissingIndexes(MetadataReader mr, StripeInformation stripe, boolean[] includes,
++      boolean[] sargColumns) throws IOException {
-    rowIndexes = mr.readRowIndex(stripe, null, includes, rowIndexes);
++    rowIndex = mr.readRowIndex(stripe, null, includes, rowIndex.getRowGroupIndex(),
++        sargColumns, rowIndex.getBloomFilterIndex());
   }

   public RowIndex[] getRowIndexes() {
-    return rowIndexes;
++    return rowIndex.getRowGroupIndex();
   }

   public void setRowIndexes(RowIndex[] rowIndexes) {
-    this.rowIndexes = rowIndexes;
++    this.rowIndex.setRowGroupIndex(rowIndexes);
   }
```
