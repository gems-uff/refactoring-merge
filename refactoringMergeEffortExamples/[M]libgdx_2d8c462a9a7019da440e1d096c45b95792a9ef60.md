# Project: libgdx — Merge commit SHA1: 2d8c462a9a7019da440e1d096c45b95792a9ef60

## Modified file(s):
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidInput.java`
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidInputImpl.java`
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidInputFactory.java` (deleted)
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidApplication.java`
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidDaydream.java`
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidFragmentApplication.java`
- `backends/gdx-backend-android/src/com/badlogic/gdx/backends/android/AndroidLiveWallpaper.java`

## Class(es) modified in the merge:
`AndroidInput`, `AndroidInputImpl`

## Merge effort lines in the combined diff

### AndroidInput.java — transformed from class to interface:
```diff
-/* full class body with 1050 lines of implementation */
-public class AndroidInput implements Input, OnKeyListener, OnTouchListener, OnGenericMotionListener {
-    ...all implementation fields and methods...
-}

+public interface AndroidInput extends Input {
+    void onPause();
+    void onResume();
+    void onDreamingStarted();
+    void onDreamingStopped();
+    boolean onTouch(View view, MotionEvent motionEvent);
+    void addKeyListener(View.OnKeyListener listener);
++    void addGenericMotionListener(View.OnGenericMotionListener listener);
+    void processEvents();
+    void setKeyboardAvailable(boolean available);
+}
```

### AndroidInputImpl.java — new class implementing the interface:
```diff
-public class AndroidInputImpl implements AndroidInput, OnKeyListener, OnTouchListener {
++public class AndroidInputImpl implements AndroidInput, OnKeyListener, OnTouchListener, OnGenericMotionListener {
...
++    private final ArrayList<OnGenericMotionListener> genericMotionListeners = new ArrayList();
++    private final AndroidMouseHandler mouseHandler;
...
++    @Override
++    public boolean onGenericMotion(View view, MotionEvent event) {
++        if (mouseHandler.onGenericMotion(event, this)) return true;
++        for (int i = 0, n = genericMotionListeners.size(); i < n; i++)
++            if (genericMotionListeners.get(i).onGenericMotion(view, event)) return true;
++        return false;
++    }
++
++    @Override
++    public void addGenericMotionListener(OnGenericMotionListener listener) {
++        genericMotionListeners.add(listener);
++    }
```

### Call sites (AndroidApplication, etc.):
```diff
+    public AndroidInput createInput(Application activity, Context context, Object view, AndroidApplicationConfiguration config) {
-        return AndroidInputFactory.newAndroidInput(this, this, graphics.view, config);
++        return new AndroidInputImpl(this, this, graphics.view, config);
+    }
```

## Relevant final code in the merge

```java
// AndroidInput.java — now an interface
public interface AndroidInput extends Input {
    void onPause();
    void onResume();
    void addGenericMotionListener(View.OnGenericMotionListener listener);
    ...
}

// AndroidInputImpl.java — the concrete implementation
public class AndroidInputImpl implements AndroidInput, OnKeyListener, OnTouchListener, OnGenericMotionListener {
    ...
    public void addGenericMotionListener(OnGenericMotionListener listener) {
        genericMotionListeners.add(listener);
    }
}
```

## Number of merge-effort lines (++ and --) associated with the refactoring types under analysis:
12 lines (`++` for `addGenericMotionListener` method declaration in the interface; `++` for `onGenericMotion` and `addGenericMotionListener` method implementations in `AndroidInputImpl`; `++` for `OnGenericMotionListener` in the `implements` clause; `++` for the `mouseHandler` and `genericMotionListeners` fields; `++` for `new AndroidInputImpl(...)` call sites replacing `AndroidInputFactory`).

## What each side had

- **Parent 1 (P1):** `AndroidInput` was a concrete class with ~1050 lines of implementation. `AndroidInputFactory.newAndroidInput()` was used at call sites to instantiate it (with SDK version branching to `AndroidInputThreePlus` or `AndroidInputImpl`). `AndroidInputImpl` existed as a subclass. `AndroidInput` had `genericMotionListeners` and `mouseHandler` fields.
- **Parent 2 (P2):** Refactored `AndroidInput` from a class into an **interface** (`Extract_Interface` refactoring). Moved the implementation into `AndroidInputImpl`. Deleted `AndroidInputFactory`. Updated all call sites to use `new AndroidInputImpl(...)` directly. However, P2's `AndroidInputImpl` did not yet implement `OnGenericMotionListener` or include the `addGenericMotionListener` method — those existed only in P1's `AndroidInput` class.

## Interpretation

This is a clear **Extract_Interface** refactoring introduced in P2: `AndroidInput` was converted from a 1050-line concrete class to a lean interface. The conflict arose because P1 had `genericMotionListeners`, `mouseHandler`, `onGenericMotion()`, and `addGenericMotionListener()` in `AndroidInput` (class), while P2's `AndroidInputImpl` did not yet include them. The merge effort (`++` lines) resolves this by: (1) adding `addGenericMotionListener` to the `AndroidInput` interface, (2) adding `OnGenericMotionListener` to `AndroidInputImpl`'s `implements` clause, (3) adding the `mouseHandler`/`genericMotionListeners` fields and both methods to `AndroidInputImpl`. The deletion of `AndroidInputFactory` and the call-site changes to `new AndroidInputImpl(...)` further confirm the **Extract_Interface** + **Rename_Class** (old `AndroidInput` class → `AndroidInputImpl`) pattern.

## Complete diff

*(See full diff in `libgdx_2d8c462a9a7019da440e1d096c45b95792a9ef60.diff`)*
